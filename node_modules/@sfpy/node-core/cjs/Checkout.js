"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.createCheckout = void 0;
const hostUrls = {
    development: "https://dev.api.getsafepay.com/embedded/",
    sandbox: "https://sandbox.api.getsafepay.com/embedded/",
    production: "https://getsafepay.com/embedded/",
};
const buildQueryParams = (params) => Object.entries(params)
    .filter(([, value]) => value !== undefined)
    .map(([key, value]) => `${encodeURIComponent(key)}=${encodeURIComponent(value)}`)
    .join("&");
function createCheckout() {
    return {
        createCheckoutUrl: (_a) => {
            var { env } = _a, params = __rest(_a, ["env"]);
            const baseUrl = hostUrls[env];
            const queryParams = buildQueryParams(Object.assign({ environment: env }, params));
            return `${baseUrl}?${queryParams}`;
        },
    };
}
exports.createCheckout = createCheckout;
